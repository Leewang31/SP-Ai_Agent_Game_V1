# 동물 숨기 — Claude Code 에이전트 설계서 V3

> **목적:** 사용자 ↔ 메인 에이전트 간 대화 기반으로, 메인이 기획/디자인/개발/리뷰 팀에 작업을 분배하고 중재하는 하향식 멀티에이전트 시스템 계획서.
> 이 문서는 Claude Code에서 실제 구현 시 참조하는 **계획서** 역할을 하며, 인터뷰를 통해 확정된 결정사항을 포함한다.

---

## 1. 작업 컨텍스트

### 배경 및 목적
사용자가 메인 에이전트에게 지시를 내리면, 메인이 판단하여 적절한 팀(서브에이전트)에 작업을 분배한다.
각 팀은 독립적으로 작업하되, 팀 간 전달·협업·에스컬레이션은 반드시 메인을 통해서만 이루어진다.
사용자는 메인 에이전트와만 대화하며, 팀 내부 작업 상황은 메인이 요약해서 보고한다.

### 엔진 선택 이유
- **Godot 4 / GDScript** 선택
- 이유: Godot이 CLI 빌드 등 에이전트 코드생성 검증 도구와 더 원활하게 통합됨
- Unity/C# 대비 헤드리스 환경에서 다루기 용이

### 소통 구조
```
[사용자]
   │  지시 / 질문 / 피드백
   ▼
[메인 에이전트] ── 작업 분배, 중재, 보고, 에스컬레이션 창구
   │
   ├──▶ [기획팀]   작업 결과 ──▶ 메인 ──▶ (필요 시) 다음 팀
   ├──▶ [디자인팀] 작업 결과 ──▶ 메인 ──▶ (필요 시) 다음 팀
   ├──▶ [개발팀]   작업 결과 ──▶ 메인 ──▶ (필요 시) 다음 팀
   └──▶ [리뷰팀]   작업 결과 ──▶ 메인 ──▶ 사용자에게 최종 보고
```

### 입력
- 사용자의 자연어 지시
- `게임_기획_명세서.md` — 이미 작성된 파일. 에이전트가 읽어 분석 (사용자가 별도 제공)

### 출력 (MVP 기준)
- `/output/godot-project/scripts/MatchManager.gd` — MVP 생성 대상 파일 (단일)
- `/output/reports/` — 각 팀의 작업 결과 보고서
- `/output/logs/escalation_log.md` — 에스컬레이션 이력
- `/output/logs/session_state.md` — 세션 상태 (단계 진행 현황)

### MVP 범위
이번 워크플로에서 개발팀이 생성하는 파일은 **MatchManager.gd 하나**다.
나머지 5개 시스템은 향후 단계에서 추가한다.

| 시스템 | 이번 MVP 포함 여부 |
|--------|------------------|
| MatchManager (타이머, 상태머신) | ✅ |
| BotController (군중 AI) | ❌ 다음 단계 |
| SafeZoneController | ❌ 다음 단계 |
| CombatSystem | ❌ 미정 (고려 안 함) |
| AnimalSwitchSystem | ❌ 다음 단계 |
| RewardSystem | ❌ 미정 (고려 안 함) |

### 성공 기준
Godot Editor에서 열었을 때 **컴파일 오류 없는** MatchManager.gd 생성.

### 주요 제약조건
- 팀 간 직접 소통 금지 — 반드시 메인을 경유
- 메인은 오류·모호한 판단 상황 발생 시 즉시 사용자에게 보고
- 사용자 승인 시점: 기획 확정, 아키텍처 확정, 최종 코드 완성 (3곳)
- 중간 산출물은 `/output/`에 파일로 저장, 팀 간 전달은 파일 경로로

---

## 2. 게임 메카닉 명세 (확정)

에이전트가 기획서를 파싱할 때 아래 사항은 이미 확정된 스펙으로 처리한다.

### 타임라인
| 시간 | 이벤트 |
|------|--------|
| 0:00 | **선진입 단계 시작** (30초) |
| 0:30 | **본 게임 시작** (3분) |
| 3:30 | **강제 동물 변경** 트리거 |
| 3:30 이후 | 변경 선택 완료 후 재개 |

### 선진입 단계 (30초)
- 3가지 랜덤 동물 중 사용자가 1마리 선택
- 선택 완료 시: 맵을 자유롭게 돌아다니며 숨을 자리 탐색 가능
- 이 단계에서 **공격 불가**, **다른 플레이어 비표시**
- 30초 내 동물 미선택 시: 랜덤 동물 자동 배정 + 초기 위치에서 시작

### 강제 동물 변경 (3분 30초 시점)
- 3가지 랜덤 동물 UI 표시
- 플레이어가 1마리 선택

### SafeZone
- **Phase 1만 구현** (Phase 2 제외)
- 숨기 및 찾기 핵심 메카닉에만 집중

### 미확정 메카닉 (이번 MVP에서 제외)
- 봇 vs 유저 공격 판정 분기
- 미니맵 노출 보상
- 승리/종료 조건
- 보트 군중 AI 알고리즘

---

## 3. 에이전트 구조

### 메인 에이전트 (CLAUDE.md) — PM·오케스트레이터

**역할**
- 사용자 지시를 해석하여 어느 팀에 무엇을 시킬지 판단
- 팀 작업 완료 결과를 수신하여 다음 팀으로 전달하거나 사용자에게 보고
- 팀에서 올라온 오류·질문을 사용자에게 전달하고 답변을 팀에 내려줌
- 전체 진행 상황을 사용자에게 요약 보고
- 각 단계 완료 후 `/output/logs/session_state.md` 갱신

**에스컬레이션 판단 기준**
- **구현 방향이 2가지 이상 존재** → 무조건 사용자에게 에스컬레이션
- 기획서에 명시된 타임라인/판정 로직 내 사항 → 자체 해결 가능
- 적용 파일 범위가 요청보다 커지는 경우 → 에스컬레이션

**사용자 승인 응답 처리 (CLAUDE.md에 명시)**
- 사용자 응답은 자유 텍스트
- 승인 키워드: `승인`, `네`, `yes`, `확인`, `좋아`, `진행해` → 다음 단계 진행
- 재작업 키워드: `수정`, `다시`, `바꿔줘`, `no`, `아니` → 해당 팀 재작업 지시
- 키워드 없이 구체적 내용이 있으면 → 수정 피드백으로 해석

---

### 서브에이전트 4팀

#### 기획팀 (planning)
| 항목 | 내용 |
|------|------|
| **역할** | 기획 명세서 분석, 요구사항 구조화, 기능 우선순위 정의, 모호한 기획 요소 정리 |
| **트리거** | 메인이 "기획 분석", "요구사항 정리", "기능 범위 확정" 작업을 내릴 때 |
| **입력** | `게임_기획_명세서.md`, 메인의 작업 지시 |
| **출력** | `parsed_spec.json` (머신 입력용), `feature_list.md` (사람 확인용 초안) |
| **에스컬레이션 조건** | 기획서에 명시되지 않은 기능 판단 필요 시, 우선순위 충돌 발생 시 |

**산출물 역할 구분**
- `feature_list.md`: 기획팀 작업 중간 초안·협의용. 사용자 승인 전 검토 문서
- `parsed_spec.json`: 사용자 승인 후 확정된 기획 데이터. 디자인팀/개발팀 머신 입력용

#### 디자인팀 (design)
| 항목 | 내용 |
|------|------|
| **역할** | Godot 씬 트리·노드 구조 설계, 시그널 흐름 정의, 데이터 구조 설계 |
| **트리거** | 메인이 "아키텍처 설계", "씬 구조 정의" 작업을 내릴 때 (기획팀 산출물 수신 후) |
| **입력** | `feature_list.md`, `parsed_spec.json` |
| **출력** | `architecture_review.md` (씬 트리, 시그널 흐름, 스크립트 목록) |
| **에스컬레이션 조건** | 기술적 구현 방향이 복수일 때 트레이드오프 판단 필요 시 |

**디자인팀 AGENT.md 핵심 결정 기준**
- **Godot 아키텍처**: Scene 노드 체계 사용 (Autoload Singleton 사용하지 않음). 상태는 로컬 씬 노드로 관리
- **시스템 간 소통 방식**: 디자인팀이 Signal 전용 / get_node() 직접 참조 / 혼합 방식 중 선택. 선택 이유를 architecture_review.md에 명시
- **노드 타입 결정 기준**: 각 .gd 파일이 extends할 노드 타입(예: CharacterBody2D, Node, Node2D 등)을 시스템별로 명세. 근거는 게임 메카닉 요구사항 기반
- **architecture_review.md 포맷**: 사람이 읽는 설명문 + 머신이 읽는 코드 스니팀 혼합. 각 클래스의 public 메서드 시그니처까지 명세하여 개발팀이 일관되게 구현 가능하도록

#### 개발팀 (development)
| 항목 | 내용 |
|------|------|
| **역할** | GDScript 코드 생성, Godot 프로젝트 구조 생성 |
| **트리거** | 메인이 "코드 생성" 작업을 내릴 때 (디자인팀 산출물 + 사용자 아키텍처 승인 후) |
| **입력** | `architecture_review.md` |
| **출력** | `/output/godot-project/scripts/MatchManager.gd`, `project.godot` |
| **에스컬레이션 조건** | 구현 불가능한 기획 요소 발견 시, 코드 생성 중 설계 충돌 발생 시 |
| **생성 방식** | **순차 생성** — 일관성 우선. 이전 파일을 참조하며 다음 파일 생성 |

**MVP 생성 파일**
- `MatchManager.gd` — 타이머, 상태머신 (선진입 30초 → 본게임 3분 → 동물변경 3:30), 동물 선택 UI 트리거

#### 리뷰팀 (review)
| 항목 | 내용 |
|------|------|
| **역할** | 생성 코드 정적 분석, 기획 요구사항 대비 누락 체크, LLM 코드 리뷰 |
| **트리거** | 메인이 "리뷰" 작업을 내릴 때 (개발팀 산출물 수신 후) |
| **입력** | `/output/godot-project/`, `feature_list.md` |
| **출력** | `code_review.md` (치명적 이슈 / 경고 / 통과 목록) |
| **에스컬레이션 조건** | 2회 재작업 후에도 치명적 이슈 남으면 메인 → 사용자 판단 요청 |

**리뷰 기준 (GDScript)**
- 기획서 요구사항 충족 여부 (LLM 판단 중심)
- 구문/문법 오류 (syntax) — 정적 분석기 `analyze_gd.py` 로 체크
- `extends` 노드 타입이 디자인팀 명세와 일치하는지
- architecture_review.md에 명시된 필수 메서드 존재 여부 (문자열 매칭)

**재작업 정책**
- 치명적 이슈 발견 → 개발팀 재작업 지시 (자동, 최대 2회)
- 2회 후에도 이슈 → 메인이 사용자에게 상황 보고 및 판단 요청

---

## 4. 워크플로우

### 기본 흐름

```
[사용자] "개발 시작해줘"
   │
   ▼
[메인] 지시 해석 → 기획팀부터 순서대로 작업 분배 결정
       session_state.md에 "단계: 기획" 기록
   │
   ▼
[메인 → 기획팀] 기획서 분석 및 요구사항 구조화 요청
   │
[기획팀] 작업 수행
   ├─ 정상: parsed_spec.json, feature_list.md 생성 → 메인에 완료 보고
   └─ 이슈: 모호한 기획 요소 발견 → 메인에 에스컬레이션
               └─ [메인 → 사용자] 질문 전달 → 답변 수신 → 기획팀에 전달
   │
   ▼
[메인 → 사용자] 기획 분석 결과 요약 보고 + 승인 요청
               session_state.md에 "단계: 기획승인대기" 기록
   │ 승인 (키워드 감지)
   ▼
[메인] session_state.md에 "단계: 디자인" 기록
[메인 → 디자인팀] 아키텍처 설계 요청 (feature_list.md 전달)
   │
[디자인팀] 작업 수행
   ├─ 정상: architecture_review.md 생성 → 메인에 완료 보고
   └─ 이슈: 트레이드오프 판단 필요 (구현 방향 2+ 존재) → 메인 → 사용자 선택지 제시
   │
   ▼
[메인 → 사용자] 아키텍처 설계 보고 + 승인 요청
               session_state.md에 "단계: 아키텍처승인대기" 기록
   │ 승인
   ▼
[메인] session_state.md에 "단계: 개발" 기록
[메인 → 개발팀] MatchManager.gd 생성 요청 (architecture_review.md 전달)
   │
[개발팀] 순차 생성
   ├─ 정상: 완료 → 메인에 보고
   └─ 이슈: 구현 불가 요소 발견 → 메인 → 사용자 또는 기획팀 재판단
   │
   ▼
[메인 → 리뷰팀] 코드 리뷰 요청
   │
[리뷰팀] 정적 분석 + LLM 리뷰
   ├─ 이슈 없음: 통과 → 메인에 보고
   └─ 치명적 이슈: 개발팀 재작업 지시 (최대 2회)
                   2회 후 실패 → 메인 → 사용자 판단 요청
   │
   ▼
[메인 → 사용자] 최종 완료 보고 (생성 파일 목록, 리뷰 결과 요약)
               session_state.md에 "단계: 완료" 기록
```

### 에스컬레이션 흐름

```
[팀에서 이슈 발생]
   │
   ▼
[팀 → 메인] 이슈 내용 + 판단 필요 사항 보고
   │
[메인 판단]
   ├─ 자체 해결 가능 (기획서에 명시된 범위 내): 메인이 직접 지시하고 팀에 전달
   └─ 구현 방향 2+ 존재 or 범위 확장: 사용자에게 상황 요약 + 선택지 제시
                          └─ 사용자 답변 → 메인 → 해당 팀에 전달
```

---

## 5. 구현 스펙

### 폴더 구조

```
/animal-hide-game
  ├── CLAUDE.md                            # 메인 에이전트 (PM·오케스트레이터)
  ├── 게임_기획_명세서.md                   # 입력 (이미 작성된 파일)
  ├── /.claude
  │   ├── /agents
  │   │   ├── /planning
  │   │   │   └── AGENT.md                 # 기획팀
  │   │   ├── /design
  │   │   │   └── AGENT.md                 # 디자인팀 (노드 타입 결정 기준 포함)
  │   │   ├── /development
  │   │   │   └── AGENT.md                 # 개발팀
  │   │   └── /review
  │   │       └── AGENT.md                 # 리뷰팀
  │   └── /skills
  │       ├── /spec-parser
  │       │   ├── SKILL.md
  │       │   └── /scripts/parse_spec.py
  │       ├── /godot-codegen
  │       │   ├── SKILL.md
  │       │   └── /references
  │       │       ├── godot4_patterns.md
  │       │       └── gdscript_style.md
  │       ├── /static-analyzer
  │       │   ├── SKILL.md
  │       │   └── /scripts/analyze_gd.py   # GDScript syntax 체크
  │       └── /project-scaffold
  │           ├── SKILL.md
  │           └── /scripts/scaffold_godot.py
  └── /output
      ├── /godot-project
      │   ├── project.godot
      │   └── /scripts
      │       └── MatchManager.gd          # MVP 생성 대상 (단일)
      ├── /reports
      │   ├── parsed_spec.json             # 기획팀 산출물 (머신 입력용, 승인 후 확정)
      │   ├── feature_list.md              # 기획팀 초안 (사람 검토용)
      │   ├── architecture_review.md       # 디자인팀 산출물
      │   └── code_review.md               # 리뷰팀 산출물
      └── /logs
          ├── escalation_log.md            # 에스컬레이션 이력
          └── session_state.md             # 세션 진행 상태 (컨텍스트 압축 복원용)
```

### session_state.md 역할
컨텍스트가 압축된 후 대화가 재개될 때 메인 에이전트가 현재 상태를 복원하기 위해 사용한다.
각 단계 완료/시작 시 메인이 직접 갱신한다.

```markdown
# 세션 상태
현재 단계: [기획 | 기획승인대기 | 디자인 | 아키텍처승인대기 | 개발 | 리뷰 | 완료]
마지막 완료 팀: [팀명]
사용자 마지막 피드백: [요약]
생성된 파일: [경로 목록]
미결 이슈: [있으면 기술]
```

### CLAUDE.md 핵심 섹션 목록
- 역할 정의 (PM·오케스트레이터, 사용자 단일 창구)
- 팀 목록 및 각 팀 호출 조건
- 에스컬레이션 판단 기준 (구현 방향 2+ → 무조건 에스컬레이션)
- 사용자 승인 응답 파싱 규칙 (승인/재작업 키워드 목록)
- 팀 간 데이터 전달 규칙 (`/output/reports/` 경로 기반)
- 사용자 승인 대기 시점 3곳 명시
- session_state.md 갱신 시점 및 형식
- 에스컬레이션 로그 기록 방식

### 각 AGENT.md 핵심 섹션 목록
- 팀 역할 및 책임 범위
- 메인으로부터 받는 입력 형식
- 메인에게 돌려주는 출력 형식
- 에스컬레이션 조건 (언제 메인에 올릴지)
- 참조 스킬 목록

---

## 6. 사용자 승인 대기 시점 3곳

| 시점 | 검토 내용 | 승인 후 | 재작업 시 |
|------|----------|---------|---------|
| ① 기획 확정 | feature_list.md 요약 — MVP 범위·타임라인·게임 메카닉 확인 | 디자인팀 작업 시작 | 피드백을 기획팀에 전달 후 재분석 |
| ② 아키텍처 확정 | architecture_review.md — 씬 구조, 노드 타입, 시그널 흐름, MatchManager API | 개발팀 코드 생성 시작 | 피드백을 디자인팀에 전달 후 재설계 |
| ③ 최종 완료 | 리뷰팀 통과 후 MatchManager.gd 결과 보고 | 프로젝트 완료 | 특정 이슈 지정 후 개발팀 재작업 |

---

## 7. 실패 처리 정책

| 상황 | 처리 주체 | 처리 방식 |
|------|----------|----------|
| 기획서 내용 모호 | 기획팀 → 메인 → 사용자 | 질문 전달 후 답변 수신해서 재작업 |
| 아키텍처 트레이드오프 (방향 2+) | 디자인팀 → 메인 → 사용자 | 선택지 제시 후 결정 수신 |
| 코드 생성 실패 | 개발팀 → 메인 | 해당 파일만 재시도 최대 2회 → 메인이 사용자에게 보고 |
| 리뷰 치명적 이슈 | 리뷰팀 → 메인 | 개발팀에 재작업 지시 (최대 2회) → 2회 후 사용자 판단 요청 |
| 팀 간 결과물 불일치 | 메인 판단 | 해당 팀에 수정 요청, 필요 시 사용자 보고 |
| 컨텍스트 압축 후 재개 | 메인 | session_state.md 먼저 읽어 현재 단계 복원 후 진행 |

---

## 8. 미결정 항목 (다음 단계에서 결정)

아래 항목은 이번 MVP에서 의도적으로 제외됐으며, 향후 단계에서 별도 인터뷰/설계가 필요하다.

| 항목 | 이유 |
|------|------|
| 봇 vs 유저 공격 판정 분기 | CombatSystem 전체가 MVP 제외 |
| 미니맵 노출 보상 | RewardSystem 전체가 MVP 제외 |
| 승리/종료 조건 | 현재 고려 안 함 |
| BotController 군중 AI 알고리즘 | MVP에서 BotController 제외 |
| SafeZone Phase 2 | Phase 1만 구현 예정 |
| 시스템 간 소통 방식 (Signal vs get_node) | 디자인팀이 architecture_review.md에서 결정 |

---

## 9. 다음 단계 (구현 시 작성할 파일 순서)

1. `CLAUDE.md` — PM 역할, 팀 호출 조건, 에스컬레이션 판단 기준, 승인 응답 파싱 규칙, session_state.md 갱신 로직
2. `/.claude/agents/planning/AGENT.md` — 기획팀 역할, 입출력, feature_list.md vs parsed_spec.json 구분
3. `/.claude/agents/design/AGENT.md` — 디자인팀 역할, Godot Scene 노드 설계 기준, 노드 타입 결정 가이드
4. `/.claude/agents/development/AGENT.md` — 개발팀 역할, 순차 생성 방식, MatchManager.gd 생성 기준
5. `/.claude/agents/review/AGENT.md` — 리뷰팀 역할, GDScript 정적 분석 기준, 재작업 2회 정책
6. `/.claude/skills/spec-parser/SKILL.md` + `parse_spec.py`
7. `/.claude/skills/godot-codegen/references/godot4_patterns.md`
8. `/.claude/skills/static-analyzer/SKILL.md` + `analyze_gd.py`
9. `/.claude/skills/project-scaffold/scripts/scaffold_godot.py`

---

## 작업 목표 #001 — 펭귄 기본 프로토타입 (2026-05-24 추가)

> 이전 MVP(큐브 플레이어 단순 이동)에서 확장. 동물 종류는 펭귄 단 한 종류.

### 핵심 컨셉
동물 숨바꼭질 게임. 플레이어와 봇이 동일한 외형(펭귄, 이번 단계는 큐브/캡슐 대체 허용)으로
맵을 돌아다니며 숨고 찾는 게임의 기본 프로토타입.

---

### 게임 오브젝트

| 오브젝트 | 설명 |
|----------|------|
| 플레이어 | CharacterBody3D. 펭귄 외형(큐브/캡슐 대체 허용) |
| 펭귄 봇 | CharacterBody3D. 동일 외형. 10마리 |
| 맵 | 50×50 크기의 평면 (바닥 + 경계 벽) |

---

### 이동 / 물리

| 항목 | 값 | 비고 |
|------|----|------|
| 기본 이동 속도 | 동일 | 플레이어 = 봇. 숨바꼭질 특성상 속도 차이 없음 |
| 달리기 속도 | 기본 × 1.8 | Shift 키 (플레이어만). 봇은 달리기 없음 |
| 점프 | 스페이스바 | 플레이어 / 봇 동일 높이 |
| 중력 | Godot 기본값 | 9.8 m/s² |

---

### 봇 AI

| 항목 | 내용 |
|------|------|
| 이동 방식 | 맵 안에서 무작위 방향으로 자유 이동 |
| 달리기 | 없음 (플레이어와 기본 이동 속도 동일) |
| 점프 | 가능 (무작위 또는 주기적으로 점프 실행) |
| 피격 시 | 즉시 쓰러짐 → 2초 후 씬에서 제거(queue_free) |
| 스폰 수 | 10마리. GameManager가 씬 시작 시 생성 |

---

### 공격 시스템

| 항목 | 값 |
|------|----|
| 공격 입력 | 마우스 우클릭 |
| 판정 범위 | 근접 2m 이내 |
| 공격 대상 | 봇만 (플레이어 간 공격 없음) |
| 피격 효과 | 대상 쓰러짐 → 2초 후 제거 |

---

### 씬 / 스크립트 목록

| 파일 | 역할 |
|------|------|
| `PlayerController.gd` | 이동, Shift 달리기, 우클릭 공격, 스페이스 점프 |
| `BotController.gd` | 자유 이동 AI, 점프, 피격 시 쓰러짐 |
| `MapGenerator.gd` | 50×50 맵(바닥 + 경계 벽) 생성 |
| `GameManager.gd` | 봇 10마리 스폰 관리 |

---

### 완료 기준

- Godot에서 실행 시 오류 없이 씬이 열릴 것
- 플레이어가 맵을 자유롭게 돌아다닐 수 있을 것
- Shift 달리기(1.8배), 스페이스 점프 동작
- 봇 10마리가 맵 안에서 자율적으로 움직일 것
- 플레이어/봇 기본 이동 속도 완전 동일
- 우클릭 시 2m 이내 봇이 쓰러졌다가 2초 후 사라질 것

---

### 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| 백업 001 | 2026-05-24 | 작업 목표 #001 반영 전 원본 백업 |
| 현재 | 2026-05-24 | 작업 목표 #001 — 펭귄 기본 프로토타입 요구사항 추가 |
